server {
    listen 80;
    server_name _;


    location /maps/ {
        root /var/www;
    }

    location /health {
        proxy_pass http://localhost:8001;
        include proxy_params;
    }
}
